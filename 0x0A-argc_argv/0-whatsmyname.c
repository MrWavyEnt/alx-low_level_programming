#include "main.h"
#include <stdio.h>
/**
 * main - prints its name
 * @argc: number of command line arguments.
 * @argv: array that contains thr command line arguments.
 * Return: Always 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
